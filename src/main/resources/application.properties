
# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/revstox_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=revuser
spring.datasource.password=revpass123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Enable schema.sql execution
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.jpa.hibernate.ddl-auto=none

# Connection Pool Settings
db.connection.pool.size=10
db.connection.timeout=30000

# CSV Data Path
csv.data.path=src/main/resources/data/NIFTY50_data.csv

# Logging Configuration
log.level=INFO
log.file.path=src/main/resources/logs/application.log

# Application Settings
app.name=RevStox
app.version=1.0.0


# Add these at the bottom of application.properties

db.url=jdbc:mysql://localhost:3306/revstox_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=revuser
db.password=revpass123
db.driver=com.mysql.cj.jdbc.Driver
